# C30D 项目迁移与 Codex 继续开发说明
交接日期：2026-09-09。项目源目录曾位于 `C:\Users\User\Documents\ChatGPT\ZTC`。

这个文件夹包含继续开发所需的 STM32 工程、ROS 2 源码、测试、教程和已保存的恢复备份。将整个文件夹复制到新电脑，作为 Codex 的项目目录打开，即可让新会话根据本说明恢复开发上下文。新会话不会自动继承旧会话的聊天、设备连接或授权状态，本包中的 `AGENTS.md` 和本说明用于补充项目背景。

## 1. 最简单的交接步骤

1. 将整个 `C30D_Development_Handoff_20260909` 文件夹或同名 ZIP 发到新电脑。
2. 若使用 ZIP，完整解压；不要直接从压缩包运行 Keil。Windows 建议解压到 `C:\Work\C30D_Project`，尽量使用较短的英文路径。
3. 在 Codex 中添加并打开这个项目文件夹，确认根目录能看到 `AGENTS.md`、本说明、`keil_project` 和 `ros2_ws`。
4. 将下一段提示发送给新电脑的 Codex。
5. 根据离线验证报告配置编译器，再决定是否连接真实设备。离线读源码、写代码和跑测试无需先连接小车。

可直接复制的提示：

```text
请接手当前文件夹中的 C30D STM32F407 + RDK X5 ROS 2 小车项目。
先完整阅读 AGENTS.md 和迁移与继续开发说明.md，核对当前主线源码、交接验证记录，以及 docs/21、22、25。
先检查文件完整性，运行可用的离线测试，确认新电脑缺少哪些依赖，并生成一份中文 Markdown 接手报告。
当前主线使用串口；CAN版本保留。IMU已有固定权重融合，EKF等只是后续方案，不能当作已实现。
后续代码使用简单语法和充分注释，技术解释给我实际 .md 文件，按数据流详细讲解。
请明确本地源码、历史验证结果和当前设备状态的区别。先完成接手核对，不自动烧录或启动电机。
```

## 2. 文件夹内容

| 路径 | 用途 |
|---|---|
| `AGENTS.md` | 新 Codex 的项目约束、用户偏好和进度摘要 |
| `README.md` | 整体功能及源码入口 |
| `keil_project/R550_C30D_SERIAL_MOTOR/` | 当前串口下位机完整工程，包含 STM32 库、FreeRTOS、驱动 |
| `keil_project/R550_C30D_CAN_MOTOR/` | 历史经典 CAN 工程，保留参考和后续开发 |
| `ros2_ws/src/chassis_can_control/` | RDK X5 上的 ROS 2 Python 包 |
| `common/`、`firmware/stm32f407/` | 独立控制核心和协议源码，供主机测试与参考 |
| `tests/`、`CMakeLists.txt` | C 控制核心离线测试 |
| `docs/` | 全部当前 Markdown 文档，含模块详解、操作手册、IMU教程 |
| `reference_materials/WHEELTEC/` | 原厂芯片资料、原理图、引脚与接线图、随货手册 |
| `hardware_backups/` | 旧电脑已读取的 MCU Flash/选项字节等备份 |
| `historical_builds/` | 已有 HEX 和构建日志，仅供历史对照 |
| `tools/` | 原有文档工具和本次打包脚本 |
| `交接验证记录.md` | 本次在交接副本上的实际检查结果 |
| `SHA256SUMS.csv` | 包内每个交付文件的 SHA-256 校验值 |

没有带入 `.git`、Keil OBJ 缓存、Python缓存、旧版重复压缩包、临时文件和视频。保留第三方源码及原有 LICENSE，原厂参考资料仅用于用户自身开发，不代表可以将所有资料公开再发布。

GD32_GCC_Firmware 是最初参考项目，当前 STM32 和 ROS 工程不依赖它编译。本包未附带整个 GD32 工程及原厂视频、操作系统安装包。若以后要重新比较 GD32 的特殊功能，需另外复制旧电脑桌面的该参考目录。

## 3. 当前硬件与项目目的

硬件为 R550 Mini 类阿克曼小车，C30D STM32F407VE 控制板、带 AB 编码器的直流减速电机、AT8236 功率驱动、转向舵机、RDK X5。板载 IMU 由程序识别 MPU6050 或 ICM20948；用户正在学习的是 ICM20948 路径。

STM32 自己执行编码器测速、前馈+PI速度环、PWM输出、命令超时和故障保护。RDK X5 接收 ROS `/cmd_vel`、发送控制命令、发布反馈和里程计。当前主线通信为 USB 串口；逻辑消息虽沿用 CAN ID，物理通道是 serial。

默认几何参数：轮径0.065m、轮距0.162m、轴距0.144m、每轮60000 count/rev。它们是源码默认值，不能代替实车标定。软件使能默认关闭。

用户希望项目具有可解释的电机控制和机器人系统工程内容，要求充分注释、浅显语法、细致中文教程。用户要求以后回答均交付 `.md` 文件。

## 4. 当前进度与未完成事项

| 内容 | 当前状态 |
|---|---|
| 串口电机控制、闭环及反馈 | 已有源码和历史实车部署记录 |
| 保留 CAN 版本 | 有独立 Keil 工程；不能假设已同步当前 IMU |
| 蜂鸣器 | 用户明确要求完全关闭，后续保持此要求 |
| IMU 采集与静止校准 | 串口主线已实现；先50样本预热，再500样本求偏差 |
| ROS IMU 发布 | 已实现，原始计数转换为 m/s²、rad/s，约50Hz |
| 当前里程计融合 | 编码器距离；gyro z与轮差转角固定加权，默认IMU权重0.85 |
| 当前设备是否正在运行最新源码 | 本次打包未连接设备，不能确认 |
| 磁力计、绝对姿态、加速度二次积分 | 当前没有进入运行时里程计融合 |
| 时间戳、全部gyro样本积分、在线偏置、动态权重 | 文档25建议，尚未实现 |
| EKF/ESKF、实际舵角反馈、激光联合定位 | 方案阶段，尚未选定并实施 |

历史文档记录过 Keil 编译与软件测试通过，不应将其等同于本次重新编译或当前实车验证。查看本次 `交接验证记录.md` 获取实际重跑范围。

## 5. 新 Windows 电脑的 Keil 配置

Keil 工程入口：

```text
keil_project/R550_C30D_SERIAL_MOTOR/USER/WHEELTEC.uvprojx
```

选中目标 `C30D_SERIAL_MOTOR`，使用 ARM Compiler 5。历史可用环境为 µVision 5.24a、ARM Compiler 5.06 update5、STM32F4xx_DFP 2.11.0。设备为 STM32F407VE，Flash 为512KiB。这些是旧环境记录；编译器、Device Pack、ST-Link驱动和有效软件许可需要在新电脑单独安装。

本包已经包含工程引用的 FWLIB、CORE、FreeRTOS、SYSTEM、HARDWARE 等源码，不需手工重新下载并拼接 STM32 固件库。Device Pack 与工程内的固件库不是同一件事。

原 `BUILD_WITH_KEIL.bat` 等脚本中的 Keil 路径写死为旧电脑 `C:\Users\User\Desktop\keil5\UV4\UV4.exe`。新电脑可直接用 µVision 打开工程构建，或按实际安装位置修改批处理中的 `KEIL_UV4`。先检查脚本再运行，尤其不要把构建脚本和烧录脚本混淆。

若某版本 IDE 要求输出目录预先存在，可在主线工程下创建 `OBJ_SERIAL`。缺少旧 `.uvguix` 用户布局文件是正常的，已故意省略；`.uvprojx`、`.uvoptx` 和项目源码保留。

## 6. 新电脑离线测试

以下命令从本交接文件夹根目录运行。Windows使用 `python`，Linux通常使用 `python3`。

```powershell
python ros2_ws/src/chassis_can_control/test/run_protocol_tests.py
```

该入口不要求安装 ROS，检查协议和里程计的核心逻辑。更全面的 Python 测试可在安装 pytest 后运行：

```powershell
python -m pytest ros2_ws/src/chassis_can_control/test
```

C 测试可在安装 GCC 后执行（Windows生成 `.exe`）：

```powershell
gcc -std=c99 -Wall -Wextra -Wpedantic -Icommon -Ifirmware/stm32f407/inc common/chassis_can_protocol.c firmware/stm32f407/src/chassis_params.c firmware/stm32f407/src/wheel_controller.c firmware/stm32f407/src/chassis_app.c tests/test_main.c -lm -o chassis_core_tests.exe
./chassis_core_tests.exe
```

Linux 使用 GCC/CMake：

```bash
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
```

`CMakeLists.txt` 链接 `m`，建议使用 GCC/MinGW 或 Linux GCC。不能直接假设 MSVC 构建也兼容。

C 测试使用独立核心，不能证明 Keil 内嵌的 `CHASSIS_SERIAL` 副本也经过同一次测试。改动后检查两套文件差异，再分别执行离线测试和 Keil 构建。

## 7. RDK X5 和 Ubuntu 的恢复开发流程

换开发电脑不要求重刷 RDK X5。如果仍使用原来的 RDK，先重新确认 IP、用户名、SSH可达性以及已运行服务，避免启动两个底盘节点抢占串口。

本交接包是开发项目，未包含RDK系统镜像、虚拟机磁盘、已安装ROS、地图和设备上未同步回来的修改。设备运行版本应通过SSH读文件、服务和进程后与本包比较。密码不写入交接说明，由用户在新会话中按需提供；历史文档可能含旧IP/用户名，本包按私人交接资料处理。

历史启动脚本使用以下目录：

```text
$HOME/chassis_project/ros2_ws
```

为了复用脚本，可将本包的 `ros2_ws` 放在 RDK 用户目录 `chassis_project` 中。目标目录已有内容时先备份比较，避免覆盖板上可能较新的源码。也可保留自己的路径并相应修改启动脚本。

在已经正确安装 ROS 2 Humble 和 colcon 的 Ubuntu/RDK 上：

```bash
cd "$HOME/chassis_project/ros2_ws"
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
```

先加载 ROS 环境，再启用 shell 的 `set -u`，否则可能再次触发 `AMENT_TRACE_SETUP_FILES: unbound variable`。缺包时按 `package.xml` 安装依赖；本说明没有宣称新系统已安装所有ROS依赖。

检查设备名：

```bash
ls -l /dev/serial/by-id/
ros2 node list
```

配置在 `config/chassis_serial.yaml`。旧串口名是 `usb-WCH.CN_USB_Single_Serial_0002-if00`，波特率115200；以现场枚举结果为准，不能将雷达串口误当底盘串口。

确认设备、无重复节点、软件使能关闭后，原操作手册使用：

```bash
ros2 launch chassis_can_control chassis_serial.launch.py
```

详细烧录和实车流程见 `docs/20_串口版上下位机完整操作与测试验收手册.md`、`docs/21_IMU融合实现与实车操作手册.md`。桌面虚拟机可用于编辑和测试；将节点真正部署在何处必须明确，USB是否透传给虚拟机也必须现场确认。

## 8. 恢复备份的边界

`hardware_backups` 是旧电脑已经保存的板级文件。包含原厂读取副本、关蜂鸣器后的副本、CAN变更前备份、串口恢复前备份和选项字节等。文件名反映历史用途，不代表当前推荐烧录版本。

SHA-256只能证明传输完整，不能证明某个bin对应当前板号或当前应用起始地址。恢复前核对MCU型号、Flash容量、Bootloader布局、备份时点和两次原厂读取是否一致。选项字节含写保护/读保护等设备配置，不要作为普通应用自动写回。

RDK恢复需要单独的SD/eMMC镜像或文件备份；MCU Flash备份不能恢复RDK。Keil生成HEX也不是RDK烧录文件。

## 9. 新 Codex 必须知道的实现细节

ICM20948 的8点平均依靠每轴各自的static环形缓冲区；外层3次循环是x/y/z。初始化32次平均产生 `gstGyroOffset`。任务中的500点校准统计已经扣掉第一层偏置后的剩余值，再保存到 `Deviation_gyro`。最终gyro = 最近8点平均 - gstGyroOffset - Deviation_gyro。它不是500点持续滑动滤波。

8点平均带来延迟；500次样本经过滑动平均后彼此相关，不能把500当作500个完全独立观测。原读取函数逐字节I2C且ACK错误未完整上报，两个读取函数有未初始化返回变量等历史问题，文档23已提醒，尚未据此修改驱动。

当前ROS融合使用最近gyro乘编码器周期，未使用MCU采样时间。编码器20Hz、IMU向量50Hz、任务100Hz；不能把消息到达时间等同于真实采样时间。参数0.85只是经验权重，没有真实精度百分比含义。

文档25是讨论路线，不是完备滤波器设计：其中5状态EKF是教学草图，实施时须明确状态转移、噪声、观测相关性和可观测性；若速度作输入又作状态，需要统一模型。`robot_localization`通用状态不含专用gyro bias状态，不能声称安装后自动估计该偏置。静止检测也不能仅凭轮速为零证明车身静止。

阿克曼关系需确认编码器所属车轴和基准点；`vy=0`只是在所选参考点及无侧滑条件下的模型约束。实际舵角反馈当前没有提供，命令PWM不能当作实测舵角。激光/视觉硬件是否可用还要核对，不能把理论架构写成部署事实。

## 10. 校验文件与后续记录

`SHA256SUMS.csv` 记录交付文件相对路径、字节数及SHA-256，不包含清单自身。解压后可在PowerShell中检查：

```powershell
$packageRoot = (Get-Location).Path
Import-Csv ./SHA256SUMS.csv | ForEach-Object {
    $targetFile = Join-Path $packageRoot $_.Path
    if (-not (Test-Path -LiteralPath $targetFile)) { throw "Missing: $($_.Path)" }
    if ((Get-FileHash -LiteralPath $targetFile -Algorithm SHA256).Hash -ne $_.SHA256) {
        throw "Hash mismatch: $($_.Path)"
    }
}
```

新电脑编辑文件后哈希变化正常，应保留原清单作为接收基准，并用Git记录后续修改。旧目录的Git未纳入，本包是文件快照；可在新目录执行 `git init` 后由用户决定首次提交。后续将环境、测试结果、部署设备及源码版本写入新的 `.md`，让下一次交接不再依赖聊天记忆。
