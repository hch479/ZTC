# 水平静止复测准备检查

用户已确认小车架空且水平。计划是复位下位机，重新完成启动校准，再采集完整60秒静止数据；不使能电机。

## 本次实际检查结果

- RDK连接仍可用，未发现底盘节点或对应launch进程。
- 尝试通过此前的ST-Link复位时，工具停在等待探针连接阶段，没有执行目标复位。
- 随后检查显示`No available debug probes are connected`；Windows当前设备列表也没有匹配的STLink/ST-Link设备。
- 已取消等待命令，不会在之后插入ST-Link时自动继续执行。
- 本次没有烧录、擦除、下发运动命令，也没有开始新一轮采样。不能把上一轮校准结果当成本次水平条件下的复测结果。

## 需要完成的操作

请将ST-Link重新连接电脑USB口，并保持其与下位机SWD接线连接。不要仅凭探针指示灯判断电脑已识别；接好后告知，再重新检查。

保持车体架空、水平且固定。确认连接后再由工具执行复位；复位后的校准和采样期间不要触碰车体。

此前烧录与回读已经成功，不需要因为本次探针未连接而重新烧录。前次结果见文档33。
