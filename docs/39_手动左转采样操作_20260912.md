# 手动左转采样操作

已确认底盘IDLE、NONE，enable=false服务返回drive disabled。没有使能电机。

录包已经实际开始，订阅到IMU、融合odom、纯编码器odom、TF和诊断，数据库已产生数据。路径：`/home/wheeltec/ekf_validation_20260911/manual_left90_20260912`。同时监听cmd_vel，但本次没有发布速度命令。

请在收到本轮开始提示后：

1. 保持静止5秒。
2. 从上方看，缓慢逆时针转动整车约90°，用约5～10秒完成。
3. 到达标记后保持静止5秒，不要转回。
4. 回复“左转完成”，并说明实际角度是否接近90°。

不要仅转动车轮或转向舵机，测的是车体旋转。不要拉扯接线或让支架失稳；若不安全立即停止动作并告知。无须给下位机重新上电。

记录设置了最长约9分钟的超时，节点最长约10分钟；不要长时间离开后直接执行动作，应先让助手确认仍在记录。录包最初因后台终端读取暂停，已恢复到前台并确认Recording后才给出动作提示。

这是IMU轴向与响应检查，结果尚待动作结束后分析，不能提前称为通过。悬空手动转车时编码器可能不滚动，融合角度不能作为正常行驶角度精度验收。
