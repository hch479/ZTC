# 2026-09-09 交接副本验证记录

检查对象是本文件所在的交接文件夹，非旧发布压缩包。

| 项目 | 结果 |
|---|---|
| 从当前工作区复制源码、文档、备份 | 成功；复制脚本逐文件比较SHA-256 |
| 原厂配套参考资料 | 已加入36个文件，约47.8MiB；按原文件字节复制 |
| Keil `.uvprojx` 的所有 `FilePath` 引用 | 缺失0项 |
| Python `test/run_protocol_tests.py` | `All Python protocol tests passed.` |
| GCC C99 编译及C测试 | `All chassis core tests passed.` |
| Python完整pytest测试 | 未执行成功：当前Python环境未安装pytest |
| 本次重新运行Keil编译 | 未执行；历史HEX/日志单独保存在historical_builds |
| 新电脑真实编译、ROS Humble运行 | 需在新电脑/RDK上验证 |
| 当前MCU固件读取或烧录、实车动作 | 未执行 |
| 当前RDK与Ubuntu虚拟机磁盘同步 | 未执行；本包不是系统镜像 |

Python解释器为旧电脑Python3.12；离线自测通过不等于Humble运行时依赖齐备。C测试使用MinGW GCC，测试对象为common与firmware/stm32f407中的独立核心。Keil运行环境按迁移说明另行配置。

源码主线保持当前状态。已剔除旧OBJ缓存和Python缓存，仅保留历史HEX/构建日志供比较。全部交付文件的完整性见根目录SHA256SUMS.csv，清单自身不纳入哈希。

打包时在docs、ROS源码与tools中按已知历史密码及常见password/passwd赋值模式检查，没有匹配结果；这不是全面机密审计。包内有设备备份和原厂资料，供用户在自己的电脑之间交接。
